# Archive

Historical technical-spec snapshots, preserved for lineage and context.

- `EastersealsIowa_TechSpec_v3.md` through `v8.md` — weekly/monthly
  refreshes of the project tech spec during the Lovable-authored build
  phase (late March – early April 2026). Each version captures the
  stack, schema, and feature set at that point in time.

**Do not treat these as current.** The canonical, maintained project
description lives in the root [`README.md`](../../README.md). The canonical
lifecycle + invariants doc is [`docs/SHIFT_LIFECYCLE.md`](../SHIFT_LIFECYCLE.md).
When in doubt, read those, not the files here.

These files mention the pre-MailerSend email provider (Resend) — that's
expected for historical accuracy. They will not be updated.
