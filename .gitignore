.env
.env.*
!.env.example

logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local config — may contain MCP server secrets (e.g. Supabase service-role key)
.claude/

# Local-only docs / operator runbooks — not for the repo
Easterseals_Tech_Spec.md
break-glass.txt

# Android signing key — never commit this.
# The historical paths below were previously committed to git history in
# commit e66d0b3 (Apr 7, 2026) and purged via history rewrite. Keeping them
# ignored here prevents a re-accident if the same filenames are regenerated.
android-release-key.jks
android-release-key.jks.bak
android/android.keystore
android/*.jks
android/*.keystore
android/app-release*.apk
android/app-release*.aab
android/app-release-signed.apk.idsig
android/app-release-unsigned-aligned.apk
android/build/
android/app/build/
android/.gradle/
